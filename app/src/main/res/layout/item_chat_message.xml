<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="android.view.View" />

        <variable
            name="chatMessage"
            type="com.example.demo02app.model.chat.data.ChatMessage" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white">

        <TextView
            android:id="@+id/tv_time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_margin="10dp"
            android:hint="2000-02-02 18:30:20"
            android:text="@{chatMessage.time}"
            android:textAlignment="center" />

        <ImageView
            android:id="@+id/iv_profile_pic_opposite"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_below="@+id/tv_time"
            android:layout_margin="10dp"
            android:src="@mipmap/profile_pic_male"
            android:visibility="@{chatMessage.isSend?View.INVISIBLE:View.VISIBLE}" />

        <TextView
            android:id="@+id/tv_content"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/tv_time"
            android:layout_toStartOf="@+id/iv_profile_pic_mine"
            android:layout_toEndOf="@+id/iv_profile_pic_opposite"
            android:minWidth="100dp"
            android:text="@string/large_text"
            android:textSize="18sp" />

        <ImageView
            android:id="@+id/iv_profile_pic_mine"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_below="@+id/tv_time"
            android:layout_alignParentEnd="true"
            android:layout_margin="10dp"
            android:src="@mipmap/profile_pic_male"
            android:visibility="@{chatMessage.isSend?View.VISIBLE:View.INVISIBLE}" />

    </RelativeLayout>
</layout>